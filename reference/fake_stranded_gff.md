# Fake stranded GFF file

Check if all the transcript in the annotation is stranded. If not,
convert to '+'.

## Usage

``` r
fake_stranded_gff(gff_file)
```

## Value

Path to the temporary file with unstranded transcripts converted to '+'.
